# CodeGraph-VBA runtime artifacts (not source-of-truth, regenerated by daemon)
# The index (codegraph.db + config.json) IS tracked per repo policy (foundation 2026-08-06).
codegraph.lock
codegraph.db-shm
codegraph.db-wal
daemon.pid
daemon.log
