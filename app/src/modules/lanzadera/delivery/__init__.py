"""Driving adapters — FastAPI HTTP routes, HTMX templates, CLI.

The `delivery` layer is allowed to import any other layer because it is the
outer edge of the hexagonal inversion (DA-1).
"""

from __future__ import annotations
